#:name:# 
#:path:# 

ct U chr_#name#_index = _index + 8 // Level CHR banks start at Bank 8, right after Bank 7 (Font)

// Standard bank
chrrom ((_index + 8) * $2000)
    file(fmt, #"path"#)
chrrom ((_index + 8) * $2000 + $1000)
    file(fmt, "../assets/sprite.png")

