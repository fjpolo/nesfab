// AUTO-GENERATED by scripts/gen_title_data.py -- DO NOT EDIT MANUALLY
// Title screen assets from assets/TitleScreen.nss + assets/TitleScreen_CHR.chr

// Title CHR bank index (MMC3 bank 5, separate from level CHR at bank 2)
ct U TITLE_CHR_BANK = 5

// Load the raw title screen CHR tiles into MMC3 bank 5 (BG slot, $0000-$1FFF)
chrrom (5 * $2000)
    file(raw, "assets/TitleScreen_CHR.chr")

// Load font into a separate bank (Index 8 = $10000)
chrrom ($10000)
    file(fmt, "assets/font.png")

// Universal Background Color
ct U TITLE_UBC = $0F

// BG sub-palette colors (3 colors per palette x 4 palettes = 12 values)
ct U[12] title_bg_palette = U[12](
    // Palette 0:  $07, $00, $37
    $07, $00, $37,
    // Palette 1:  $00, $37, $10
    $00, $37, $10,
    // Palette 2:  $15, $0F, $0F
    $15, $0F, $0F,
    // Palette 3:  $0F, $0F, $0F
    $0F, $0F, $0F
)

// Nametable + attributes loaded from binary .nam file (960 + 64 = 1024 bytes)
data /title
    [] title_nam
        file(raw, "assets/TitleScreen.nam")
